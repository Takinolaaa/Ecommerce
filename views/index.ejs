
<table border = "2 " width="100%">

<tr><td>
<% if (allrecs.length > 0) {%>
    <% allrecs.forEach((recref, index) => { %>
    <product>
    <table>
    <tr><td><%= recref.productname %></td></tr>
    <tr><td><%= recref.prodimage %></td></tr>
    <tr><td><%= recref.category_id %></td></tr>
    <tr><td><%= recref.supplier_id %></td></tr>
    <tr><td><%= recref.prodprice %></td></tr>
    <tr><td><%= recref.status %></td></tr>
    <tr><td><a href='/product/<%= recref.product_id
    %>/show'>Details</a></td></tr>
    </table>
    </product>
    <% }); %>
<% } %>


</td>

</td>
<td width="20%">
<% if (promos.length > 0) {%>
<table>
<% promos.forEach((recref, index) => { %>
<tr><td> <img src = "/images/<%=recref.promoimage %>" width="230" height="180" ></td></tr>
<% }); %>
</table>
<% } %>
</td>
</tr></table>